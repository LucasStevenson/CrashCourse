## Toolhouse agent configuration for backend/app.py
# Copy this file to .env and fill in values.

# Your public ingest URL for the Toolhouse agent
TOOLHOUSE_URL=https://agents.toolhouse.ai/f861cfe5-362d-44c8-918b-a853b380dd66

# Optional: API key if your agent requires authorization
# TOOLHOUSE_API_KEY=sk_your_key_here

# Minimum seconds between forwards (only when cue changes)
TOOLHOUSE_MIN_INTERVAL_S=1.0

# Payload style sent to Toolhouse: 'wrapped' (recommended), 'wrapped_input', or 'raw'
# wrapped       -> sends instruction + observation JSON in a 'message' field (matches your working curl)
# wrapped_input -> same instruction but in an 'input' field
# raw           -> sends the observation JSON as-is
TOOLHOUSE_PAYLOAD_STYLE=wrapped
